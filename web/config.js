// バックエンドAPIの接続先。
//   同一オリジン配信なら空文字。別ホスト(Render等)なら公開URLを設定。
window.__API_BASE__ = "https://geiyo-bus-api.onrender.com";

// 通勤の往復定義（このアプリはこの2区間だけを表示する）。
//   from に停まった後、同じ便が to にも停まる便＝その方向、として自動判定する。
//   （direction_id や のりば を推測しないので上下・のりば違いが起きない）
//   名前は部分一致（"才の瀬" は "才の瀬橋" にマッチ）。
window.__COMMUTE__ = [
  { label: "往路　才の瀬 → 日下橋", from: "才の瀬", to: "日下橋" },
  { label: "復路　日下橋 → 才の瀬", from: "日下橋", to: "才の瀬" },
];
